{
  "name": "server",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/main.ts --outfile server",
    "dev": "bun run --hot src/main.ts",
    "start": "bun run dist/main.mjs"
  },
  "dependencies": {
    "@elysia/cors": "^1.4.2",
    "@elysia/openapi": "^1.4.15",
    "@soma/auth": "workspace:*",
    "@soma/db": "workspace:*",
    "@soma/env": "workspace:*",
    "dotenv": "catalog:",
    "elysia": "latest",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@soma/tsconfig": "workspace:*",
    "typescript": "catalog:",
    "@types/bun": "catalog:",
    "tsdown": "^0.22.3"
  },
  "module": "src/main.js"
}